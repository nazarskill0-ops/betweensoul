import { RegisterForm } from "@/features/auth/components/RegisterForm";

export const metadata = { title: "Реєстрація — Calmi" };

export default function RegisterPage() {
  return <RegisterForm />;
}
