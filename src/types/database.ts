/*
  Generated Supabase types go here. This is a placeholder.

  After you apply the schema (supabase/migrations/0001_init.sql), run:

    npx supabase gen types typescript --project-id <your-project-id> > src/types/database.ts

  Then pass `Database` into the Supabase clients for full type safety on every
  query. Re-run after each migration. See docs/DATA_MODEL.md.
*/
export type Database = unknown;
