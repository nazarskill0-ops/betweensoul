# ui/

Shared, dumb, reusable UI primitives (Button, Input, Card, Select...).
Recommended: install shadcn/ui here with `npx shadcn@latest init` then
`npx shadcn@latest add button input ...`. These components are styled with the
design tokens in `app/globals.css`. No business logic lives here.
